import { Cormorant_Garamond } from "next/font/google";

export const premiumHeading = Cormorant_Garamond({
  subsets: ["latin", "cyrillic"],
  weight: ["400", "600"],
  style: ["normal", "italic"],
  display: "swap",
});
